// Configuration file for the C-PACE Market Matrix application

const config = {
  // Application settings
  app: {
    name: "C-PACE Market Matrix",
    version: "1.0.0"
  },
  
  // Add other configuration sections as needed
};

export default config; 
